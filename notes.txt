land to login page
made random creds and then landing page!
ss


API8:2023 Security Misconfiguration (The Developers at DVAPI are lazy which has led to a misconfigured system. Find the misconfiguration and submit the flag !!!)
adding a note on your profile
make sure burpsuite is on
captured request and send to repeater to play with
ss
check accepted https request by "OPTIONS"
ss
testing other methods to see behavior
nothing worked
i tried post to see normal behavior and noticed the response
then using jwt editor extension i edited jwt token
ss
(i had already created an account named "test1")
then i got this response and we have got the flag
ss
its exposing more of details
this is "Security Misconfiguration" vulnerability
we were about to go for challenge 1 but we accidentally solved challenge 8 lol


"API1:2023 Broken Object Level Authorization" (Drop off during a CTF challenge? No problem. Store a secret note on your profile to track your progress and resume where you left off.)
vewing my profile and capturing the request to see how many requests its making
we have to keep an eye of request thats loading the note of that user
here is that request
ss
as u have guessed, that change the username in that request
(we have already added a random note to another account for testing purpose)
ss
also change jwt to match with username we entered
didnt work
send request to repeater then some experiments and found that, we have to change username only and keep jwt token similar bcz modifying jwt is causing errors (as we have alr checked)
ss
but where is the flag??
lets try modifying in the /profile request in burpsuite
we can see we got the note of another user here tooo
but no flag?
lets try getting note of another users maybe we can get flag there?
u can see list of users in scoreboard
we know admin always got something, so checking admin
ss
there is our flag!

API9:2023 Improper Inventory Management (There was a data leak at DVAPI. People found out there are 12 Challenges and not 10, What do you think ?)

capturing /challenges page request and checking all requests one by one
ss
to retrieve challenges, the common method should be GET? why post is being used? and a parameter too, lets try playing with it in repeater
first testing normal behavior
ss
we got total 10 challenges.
not modifying parameter "released" to 0 or 2
naa nothing worked
what if we change the parameter name itself?
it worked and we got 2 unreleased challenges
ss
as well as the flag too


API3:2023 Broken Object Property Level Authorization (Ever wished there was a cheat code to top the scoreboard?)
on inspecting homepage and captured request for /scores
we see
ss
by this we can guess there is a table in db that has username and scores both...
what if we are signing up and also provide scores for the  user?
capture signup req and send to repeater
ss
add a parameter as "scores"
it workedd...
lets check /scores again
we got 9999 scores
on scrolling we got the flag
ss


API5:2023 Broken Function Level Authorization (DVAPI has many users. You can see other's profile and others can see yours. What could go wrong here? Right? Right???)
on "/api/user/Charlie" path (capture from /user/Charlie page in scoreboard)
if we check OPTIONS we see option of DELETE
ss
experimnenting with it
we deleted the user and got the flag
ss








